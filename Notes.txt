
Create the authentication method in the login controller.

Show a general error message regardless of a validation error from email address or password.

Created a dashboard controller.

Created an index view for the dashboard.

Added middlewares to the routes.

Modified the 'boostrap/app.php' file to redirect unauthenticated users to the login page.

Added a 'guest' and 'auth' middleware group to the routes.

Added a logout route and its corresponding method in the login controller.

Fixed to be able to close the user dropdown when clicked away from it.

Applied 'x-cloak' attribute to the user dropdown.
