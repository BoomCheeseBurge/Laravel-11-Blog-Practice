
Added a SoftDelete trait to the Post model.

Created a migration file for 'deleted_at' field creation to the posts table.

The migration command is 'php artisan make:migration add_deleted_at_field_to_posts_table'

________________________________________________________________

Autofocus does not work in the login and contact page.

Autofocus only works if the page is reloaded on the all posts admin dashboard page.

________________________________________________________________

Added a switch between archived and active posts for the admin user.

________________________________________________________________

Added resizable table columns albeit won't work for subsequent livewire paginations.
